# this section is for creating a new branch, and switching into it immediately
git checkout -b branchname

# this section is for checking what branches exit locally
git branch

# this is how you switch into a branch you are not in
git checkout branchname
